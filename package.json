{
  "name": "siberkampus",
  "version": "1.0.0",
  "description": "Siber Kampüs siber güvenlik eğitim platformu web sitesi.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "esbuild sk-data.jsx --outfile=sk-data.js --minify --jsx=transform && esbuild sk-articles.jsx --outfile=sk-articles.js --minify --jsx=transform && esbuild app.jsx --outfile=app.js --minify --jsx=transform && esbuild app-auth.jsx --outfile=app-auth.js --minify --jsx=transform && esbuild app-pages.jsx --outfile=app-pages.js --minify --jsx=transform"
  },
  "keywords": [
    "siberkampus",
    "ctf",
    "react"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.29.7",
    "@babel/core": "^7.29.7",
    "@babel/preset-react": "^7.29.7",
    "esbuild": "^0.28.0"
  }
}
